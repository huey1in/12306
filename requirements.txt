# 12306 火车票订票工具依赖包

# 核心依赖
requests>=2.28.0
gmssl>=0.4.1

# 可选依赖（增强功能）
cryptography>=39.0.0
pillow>=9.0.0

# 开发工具（可选）
pytest>=7.0.0
black>=23.0.0
flake8>=5.0.0
pylint>=2.16.0

# 注意事项:
# 1. gmssl 用于 SM4 密码加密
# 2. requests 用于 HTTP 请求
# 3. cryptography 用于增强的加密功能
# 4. pillow 用于验证码处理（如果需要）
# 5. 开发工具用于代码质量检查和测试
